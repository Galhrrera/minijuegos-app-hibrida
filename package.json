{
  "name": "minijuegos-app-hibrida",
  "version": "1.0.0",
  "description": "proyecto final apps móviles",
  "main": "index.js",
  "scripts": {
    "clean:dev": "rimraf ./dist",
    "clean:prod": "rimraf ./dist",
    "preinstall": "npm install parcel-bundler",
    "dev": "npm run clean:dev && parcel ./src/*.html",
    "prod": "npm run clean:prod && parcel build ./src/*.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.69.5"
  }
}
